generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Job {
  id       Int     @id @default(autoincrement())
  name     String
  schedule String
  url      String
  createdAt DateTime @default(now())
}
